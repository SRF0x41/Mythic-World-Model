You are given the raw text of an ingested source. Extract its title.

## Instructions

1. If the text contains a clear title (e.g., headline, article title, post title), return it exactly as written.
2. If there is no discernible title, read the content and generate a concise, descriptive title that captures the main topic in 5-12 words.

## Rules

- Return ONLY the title, nothing else. No quotes, no labels, no explanation.
- Preserve the original capitalization if extracting an existing title.
- Do not include subtitles or bylines — only the primary title.
- If generating a title, use sentence case and keep it under 12 words.

The source text follows:
